use("commerce");
db.produtos.find({});

use("commerce");
db.produtos.find({
  nome: {$nin: ["Big Mac", "McChicken"]  }
}, { _id: 0, nome: 1, curtidas: 1, vendidos: 1 });

use("commerce");

db.produtos.find({
valoresNutricionais: {
   $elemMatch:{ 
    $and: [
      { tipo: "proteínas" }, 
      {percentual: {$gte: 30, $lte: 40}}] }}},
  { _id: 0, nome: 1 });

